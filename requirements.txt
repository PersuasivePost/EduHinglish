# ============================================================
# EduHinglish — Python 3.10 Dependencies
# ============================================================
# Install: pip install -r requirements.txt
# ============================================================

# --- PDF Processing ---
pdfplumber==0.11.0
PyPDF2==3.0.1

# --- Core NLP ---
nltk==3.9.1
spacy==3.7.4

# --- Data ---
pandas==2.2.0
numpy==1.26.4

# --- Indic Language Tools ---
indic-transliteration==2.4.1
# Note: ai4bharat-transliteration may need separate install
# pip install ai4bharat-transliteration

# --- Language Detection ---
langdetect==1.0.9

# --- ML / Transformers (for LID model later) ---
transformers==4.40.0
torch==2.2.0
sentencepiece==0.2.0
tokenizers==0.19.1

# --- Vector DB (for RAG — Phase 2) ---
chromadb==0.4.24
sentence-transformers==2.7.0

# --- Fine-tuning (for Phase 3 — LLaMA) ---
peft==0.10.0
bitsandbytes==0.43.0
trl==0.8.6
accelerate==0.29.3
datasets==2.19.0

# --- Evaluation ---
evaluate==0.4.1
rouge-score==0.1.2
bert-score==0.3.13

# --- API (for prototyping with GPT) ---
openai==1.25.0

# --- Web Interface (Phase 5) ---
streamlit==1.33.0
fastapi==0.111.0
uvicorn==0.29.0

# --- Utilities ---
python-dotenv==1.0.1
tqdm==4.66.2
colorama==0.4.6
tabulate==0.9.0

# --- Notebooks ---
jupyter==1.0.0
ipykernel==6.29.4

# ============================================================
# QUICK INSTALL for current phase (preprocessing only):
# pip install pdfplumber PyPDF2 nltk spacy pandas numpy
#             indic-transliteration langdetect colorama tabulate
# python -m spacy download en_core_web_sm
# ============================================================